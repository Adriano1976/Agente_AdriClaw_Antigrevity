/**
 * SkillExecutor: Responsável por processar e executar a lógica interna de uma skill.
 * Atualmente serve como base para futuras implementações de macros e comandos dinâmicos.
 */
export class SkillExecutor {
  // Executor estático que pode processar macros no futuro.
  // Atualmente a lógica de injetar a string no master prompt ocorre no Router/Controller
}
